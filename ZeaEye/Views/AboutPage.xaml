<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ZeaEye.Views.AboutPage"
             BackgroundImageSource="gradient_background"
             Title="{Binding Title}">

        <ScrollView>
            <Grid RowDefinitions="Auto, 30, Auto, 20, Auto, Auto"
                  VerticalOptions="Center">
                <Image Grid.Row="0"
                       HeightRequest="120"
                       WidthRequest="120"
                       Aspect="AspectFit"
                       Source="logoWithoutName"/>
                <Label Grid.Row="2"
                       Text="Ohoy sailor!"
                       HorizontalOptions="Center"
                       TextColor="#000000"
                       FontAttributes="Bold"
                       FontSize="24"/>
                <Label Grid.Row="4"
                       Text="Welcome aboard to your"
                       HorizontalOptions="Center"
                       TextColor="#7F7F7F"
                       FontSize="16"/>
                <Label Grid.Row="5"
                       Text="new personal sailing assistant"
                       HorizontalOptions="Center"
                       TextColor="#7F7F7F"
                       FontSize="16"/>
            </Grid>
        </ScrollView>
</ContentPage>
